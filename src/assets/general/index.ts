import * as images from './images';
import {colorsSocial} from './colors';
import * as responsive from '../../helpers/responsive/Index';
import {theme} from './theme';

export default {
  images,
  colorsSocial,
  responsive,
  theme,
};

export {images, colorsSocial, responsive, theme};
